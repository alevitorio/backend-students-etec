{
  "compilerOptions": {
    "target": "ES6",                        // A versão de saída do JavaScript (ES6)
    "module": "commonjs",                    // O tipo de módulo para o Node.js
    "lib": ["es6"],                          // Bibliotecas para o ambiente de execução
    "allowJs": true,                         // Permite compilar arquivos .js também
    "outDir": "./dist",                      // Diretório de saída dos arquivos JS compilados
    "rootDir": "./src",                      // Diretório raiz onde estão os arquivos TypeScript
    "esModuleInterop": true,                 // Habilita a interoperabilidade de módulos ES
    "strict": true,                          // Habilita todas as verificações de tipo rígidas
    "skipLibCheck": true,                    // Ignora a checagem de tipos de bibliotecas
    "forceConsistentCasingInFileNames": true // Garante que os nomes de arquivos sejam consistentes em maiúsculas/minúsculas
  },
  "include": ["src/**/*.ts"],                 // Inclui todos os arquivos TypeScript dentro da pasta src
  "exclude": ["node_modules", "dist"]         // Exclui node_modules e a pasta dist
}
